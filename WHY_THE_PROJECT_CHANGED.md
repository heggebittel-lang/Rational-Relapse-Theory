# Why the Project Changed — and Why It Stops Here

## 一份研究回顾

这不是一个新的理论版本，也不是对 v1.0–v9.0 的事后改写。它只回答两个问题：**为什么每一次都进入了下一个版本？为什么最后停下来？**

## 我们到底在研究什么？

回头看，项目真正持续不变的研究对象并不是“成瘾”、某个新的恢复效用函数、路径加法、补偿比较本身，也不是某一种动态系统。更稳定的问题是：

**当目标环境中缺少直接的局部反事实比较时，现有选择数据到底识别了什么；还缺什么信息；什么额外、可实施的比较支持能够严格缩小目标响应的识别集？**

最初，这个问题被写成“理性个体为什么会退出、复发、再次退出”；后来被写成恢复价值、路径历史、可加测量、补偿选择和递归延续价值。到最后，问题被剥离到最小形式：参考环境有局部补偿比较，目标环境只有基准选择而缺少邻近比较，我们能否识别目标选择对状态变化的局部响应？

因此，这个项目最深的主题其实是**缺失的反事实信息**，以及如何区分“数据真正揭示的对象”与“模型、记号或维护假设替我们填进去的对象”。

## 为什么进入下一个版本？

| 转换 | 当时发现的问题 | 为什么需要下一个版本 |
|---|---|---|
| v1.0 → v2.0 | 最初只是一个短的理性复发构造，经济故事存在，但结构不完整。 | 把直觉扩展成完整动态模型，检查稳态、比较静态和动态路径是否真的一致。 |
| v2.0 → v3.0 | “退出/恢复项”的具体函数形式过于特殊，很多结果可能只是函数形式的产物。 | 用函数族和更一般的调整结构测试机制是否依赖特定参数化。 |
| v3.0 → v4.0 | 更根本的问题出现：恢复价值与已有价值项是否重复计算？加法分解凭什么成立？ | 从偏好与表示角度重建模型，把“恢复”从一个任意附加项变成需要被表示和识别的对象。 |
| v4.0 → v5.0 | 直接给一个恢复函数仍然可能把要证明的结构预先写进模型。 | 把 primitive 改成目标相对路径和 path-generated ledger，研究在非任意可加的域上，什么加法结构能从行为条件推出。 |
| v5.0 → v6.0 | 第一次 JME 投稿（JME-S-26-00480）的核心批评是：技术构造和反例有意思，但主表示定理依赖很强的 global extension 与 Archimedean 假设，这些假设已经在很大程度上提供了结论所需的 ordered-group 结构。 | 不再把“大表示定理”当唯一目标，而把问题拆开：先问路径历史究竟有哪些信息可以被**精确压缩**，再把 passage compression 与 additive representation 分开。 |
| v6.0 → v7.0 | 即使历史压缩写得更干净，仍然有一个经济学问题没有正面回答：这些对象究竟如何被观察？ | 回到 revealed preference 和 compensated choice，用精确货币切换阈值作为可观察量，把抽象历史语言翻译成选择数据。 |
| v7.0 → v8.0 | 第二次 JME 投稿（JME-D-26-00519）的批评更直接：大量新术语重新命名了熟悉结构；翻译回标准语言后，若干结果接近 increasing differences、integrability、path independence 等成熟工具；同时“第二个实验”的经济实施不够可信。 | 去掉重新命名，承认标准工具；把两个实验收缩到同一个 monetary protocol 下的递归识别问题，试图让额外信息不再依赖一个难以解释的独立实验。 |
| v8.0 → v9.0 | 继续审计后发现，递归传播、切空间、秩/核、图结构本身仍然不是应当宣称的新贡献，而且旧的 fiber-level 信息集比真正想问的问题更强。 | 再缩一步：只研究 **current-only observational equivalence**。把标准数学工具全部降为基础设施，只问“何时当前数据已经足够；何时 enriched comparison support 真正增加识别信息”。 |
| v9.0 → 最终基础稿 | 到这里，继续增加一般化已经不能解决剩余的不确定性。剩下的问题不是还能证明多少，而是候选 structural iff 是否已有直接先例，以及现实制度是否自然提供 recombinable continuation support。 | 不再产生 v10。把项目从“继续造理论”切换为“审计先例与实施条件”；在这两点没有新的外部证据之前，继续扩展只会重新制造术语和表面新颖性。 |

## 两次拒稿真正改变了什么？

第一次拒稿没有说“数学是错的”。它指出的是更难处理的问题：**结论究竟是由数据和行为条件推出的，还是由强维护假设预先放进去的？** 这使项目从“证明一个更大的表示定理”转向“先分清哪些结构是精确压缩，哪些结构需要额外假设”。

第二次拒稿又把问题推进了一层：即使一个结果正确，如果它只是把熟悉的经济学和数学结构换了名字，或者依赖一个难以解释的实验支持，它仍然不能形成足够可信的独立贡献。于是后面的版本开始主动使用标准术语，并把 compensated comparison、path independence、cancellation、rank/kernel、cycle space、finite differences 和 implicit-function comparative statics 明确放回背景工具的位置。

从这个角度看，两次拒稿并不是把同一篇论文连续否定两次，而是分别击中了两个不同层次：第一次是**assumptions versus identification**，第二次是**terminology/contribution versus economic implementation**。后来的 v8、v9 和最终基础稿，基本就是对这两类批评的逐条消化。

## 最后到底剩下了什么？

最终基础稿把最稳的内容压缩成一个信息比较问题。目标局部响应在仅当前数据下何时被识别，可以写成观察等价扰动上的核包含条件；若该条件失败，同样的当前数据与同样的目标基准选择可以对应不同的响应。若制度允许在同一 current consequence 下重新配对不同 continuation consequence，则同一个货币补偿协议可以生成消去 current-side nuisance effects、但保留 continuation contrasts 的比较；两个缩小的标准有限差分块恰好提供目标响应所需的局部曲率，而无需恢复 continuation value 的水平。

这里真正可能有研究含义的，不是核空间、秩公式、图循环或有限差分，而是三件更窄的事情：**current-only 数据结构性地何时不足；什么可实施的 enriched support 严格缩小 identified set；以及精确响应究竟需要多少 continuation information。**

## 为什么最后停下来？

项目停在这里，不是因为最后什么都没有成立。相反，是因为到最后已经能够比较清楚地说出：哪些东西成立，哪些只是标准工具，哪些还可能是贡献，哪些必须靠文献和制度事实才能决定。

继续写下一个版本，只有在解决下列两类外部问题时才有意义：一是 prior-art audit 显示 current-only structural identification 与 strict information comparison 确实存在尚未被覆盖的缺口；二是能够给出自然、可信、不是为定理临时制造出来的经济制度，使 recombinable continuation support 真正可实施。若这两点都没有新的证据，再增加定理、维度、动态系统或新名词并不会增加知识，只会增加包装。

所以，**被放弃的不是“反事实选择识别”这个问题，而是继续把这条路线包装成一个独立的、不断扩张的“新理论”的做法。**

从 v1.0 到 v9.0，最重要的进步也许不是定理越来越多，而是 claim 越来越少、越来越准确：从“我发现了一种新的理性复发机制”，到“这里有一个路径表示问题”，再到“这里有一个补偿选择问题”，最后收缩为“这里有一个关于缺失比较和信息支持的识别问题”。

这也是为什么这个仓库保留所有中间版本。它们记录的不是九次对同一个贡献的重复声明，而是一个研究问题在批评、拒稿和文献审计中不断改变自己的对象，直到不能再靠内部推导决定下一步为止。

## 最后的判断

如果未来有人继续这条路线，最值得继承的不是内部术语，而是一个简单的研究纪律：**先问目标对象是否真的由现有数据识别；再问额外信息来自哪里；最后才问这件事是不是新的。**

在没有新的 prior-art 结论或新的可实施制度之前，v9.0 是合适的终点。这个项目因此被归档，而不是继续被改名。